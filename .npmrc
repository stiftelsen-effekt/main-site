; next 16.3.0-preview.5 is a prerelease, so it fails every peer range on "next"
; (e.g. cookies-next's >=15.0.0) and sends npm's resolver into runaway ERESOLVE
; backtracking. Skip peer resolution until we're back on a stable next.
legacy-peer-deps=true
